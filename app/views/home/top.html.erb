<div class="top-main">

    <div class="header-top">
        <div class="header-logo">
            <%=link_to("Nostalium","/")%>
        </div>
    </div>

    <div class="bg-slider">
        <%= render partial: 'login_form' %>
        <%= render partial: 'signup_form' %>
        <div class="top-message">
            <h2>Share a moment of memories</h2>
            <p>Let's remember your memory from nostalgic photos!</p>
            <button type="button" class="btn btn-primary btn-lg bule"  data-toggle="modal" data-target="#LoginModal">Login</button>
            <button type="button" class="btn btn-success btn-lg green"  data-toggle="modal" data-target="#SignUpModal">SignUp</button>
        </div>
    </div>

</div>
